import { CategoryGrid } from "@/components/category-grid";

export default function CategoriesPage() {
  return (
    <CategoryGrid
      title="All shopping categories in one place"
      intro="Choose a department to open its dedicated category page and explore only the products that belong there."
    />
  );
}
